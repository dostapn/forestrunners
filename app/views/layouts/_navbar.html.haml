%nav.navbar.navbar-default.navbar-custom.navbar-fixed-top
  .container-fluid
    .navbar-header.page-scroll
      %button.navbar-toggle{"data-target" => "#navbar-collapse-1", "data-toggle" => "collapse", :type => "button"}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand{:href => root_url} ForestRunners Club
    #navbar-collapse-1.collapse.navbar-collapse
      %ul.nav.navbar-nav.navbar-right
        - if user_signed_in?
          %li
            = link_to current_user.email, edit_user_registration_path, :class => 'fa fa-user'
          %li
            = link_to "Выйти", destroy_user_session_path, method: :delete, :class => 'fa fa-sign-out'
        - else
          %li
            = link_to "Зарегистрироваться", new_user_registration_path, :class => 'fa fa-unlock'
          %li
            = link_to "Войти", new_user_session_path, :class => 'fa fa-sign-in'
